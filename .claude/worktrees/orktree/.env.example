# Anthropic API key — used to summarize YouTube transcripts with Claude Haiku
ANTHROPIC_API_KEY=sk-ant-...

# Reddit and YouTube are fetched via their public APIs (no credentials needed).
# Optional: override server port
# PORT=8000
